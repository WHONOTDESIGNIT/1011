# robots.txt - iplmanufacturer.com
#
# Policy: ALLOW EVERYTHING ON PUBLIC CONTENT, INCLUDING EVERY AI CRAWLER.
#
#   This file is deliberately vendor-neutral. There are no per-vendor AI rules
#   and no allow/deny split between "AI search" and "AI training" crawlers:
#   the wildcard group below covers every current and future crawler, so no
#   crawler ever needs to be added here by name - and none can be forgotten.
#
#   Only internal / non-content paths are excluded. They are not pages and
#   excluding them saves crawl budget; they are not AI-related.
#
#   Guarded at build time by scripts/check-robots.mjs: the build fails if the
#   wildcard group stops allowing / (or if any group globally blocks a crawler).
#
# Last updated: 2026-09-15

User-agent: *
Allow: /
Disallow: /api/
Disallow: /admin/
Disallow: /private/
Disallow: /upload
Disallow: /.netlify/
Disallow: /index.backup/

Sitemap: https://iplmanufacturer.com/sitemap.xml
