# 罗马尼亚语（ro）兼容性与功能测试结果记录

项目：iShine Technology（iplmanufacturer.com）
测试日期：2026-08-13
测试环境：Windows + Playwright（Chromium / MS Edge / Firefox / WebKit）

## 一、兼容性测试（要求：特殊字符、排版、不同设备与屏幕尺寸）

### 1.1 测试矩阵
- 引擎：Chromium、MS Edge、Firefox、WebKit（Safari 内核）
- 视口：1920×1080（桌面大屏）、1366×768（笔记本）、375×667（iPhone SE 类）、390×844（iPhone 14 类）
- 页面：`/ro/`（首页）、`/ro/products/lumi/`（产品详情）、`/ro/faq/`（FAQ）

### 1.2 校验项与结果（48/48 通过）

| 校验项 | 判定标准 | 结果 |
|---|---|---|
| 语言声明 | `html lang="ro"` | 48/48 通过 |
| 文字方向 | `dir="ltr"`（罗马尼亚语为从左到右） | 48/48 通过 |
| 无横向溢出 | `scrollWidth ≤ clientWidth`（断行/排版正常） | 48/48 通过 |
| 无乱码 | 文档中无 U+FFFD 替换字符（�） | 48/48 通过 |
| 罗马尼亚语渲染 | 页面含 "Produse IPL / Clienți / Întrebări frecvente / Despre noi" 等标记 | 48/48 通过 |

### 1.3 特殊字符专项
- 变音符 ă â î ș ț（含大写 Ă Â Î Ș Ț）在 4 引擎下均正常渲染，无豆腐块/替换字符
- 罗马尼亚语引号 „…” 正常显示
- 小数/千位分隔（1.999.998、5,0）随上下文正常呈现

## 二、功能测试（要求：语言切换流程、无遗漏）

### 2.1 语言切换流程（4/4 引擎通过）
流程：英文首页 `/` → 打开语言切换器 → 点击 "Română" → 断言：
1. URL 跳转至 `/ro`（无 `/ro/ro` 双前缀缺陷）
2. `document.documentElement.lang === 'ro'`
3. 页面渲染罗马尼亚语内容（导航标记命中）

| 引擎 | 跳转路径 | lang | 罗马尼亚语渲染 | 结果 |
|---|---|---|---|---|
| Chromium | /ro | ro | ✓ | 通过 |
| MS Edge | /ro | ro | ✓ | 通过 |
| Firefox | /ro | ro | ✓ | 通过 |
| WebKit(Safari) | /ro | ro | ✓ | 通过 |

### 2.2 翻译覆盖（无未翻译遗漏）
- `check_ro_coverage.cjs`：键覆盖率 **100%**（4081/4081），缺失 0、多余 0
- 英文残留扫描（排除品牌/缩写/枚举白名单）：**0**
- `[TODO]` 遗留 310 处与 en.json 逐字一致，均为未被站点引用的历史空壳键（既有语言包同约定）

### 2.3 回归验证
- 10 语言全量构建成功（退出码 0），既有 9 语言页面正常生成
- sitemap.xml 新增 `/ro/` 条目 50 条
- 语言偏好持久化修复后，`localStorage` 中 `ishine_lang=ro` 时访问 `/ro/` 不再触发二次重定向

## 三、发现并修复的缺陷

| # | 缺陷 | 影响 | 修复 |
|---|---|---|---|
| 1 | 22 处语言偏好脚本 `LANGS` 数组缺 `'ro'` | 切换至 `/ro` 后二次重定向 `/ro/ro`（404），语言切换断裂 | 22 个文件数组加入 `'ro'`，重测 4 引擎全部通过 |
| 2 | 12 处 locale 推断链缺 `/ro/` 分支 | 极端场景下（currentLocale 缺失）语言判断回退英文 | 推断链插入 `startsWith('/ro')` 分支 |

## 四、结论

- 兼容性：罗马尼亚语特殊字符、排版在 4 浏览器引擎 × 4 屏幕尺寸下无乱码、无换行/溢出异常
- 功能：语言切换流程（EN ↔ RO）无缝流畅，翻译覆盖 100%，无未翻译遗漏文本
- 全部测试通过（48/48 视口用例 + 4/4 引擎切换用例），可交付上线
